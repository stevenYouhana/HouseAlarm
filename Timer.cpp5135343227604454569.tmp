#include "Arduino.h"
#include "Timer.h"

//Timer::Timer(){
//
//}
void Timer::checkForMinute(){
  while(minute != 60){
    minute++;
    Sleep(1000);
    if(minute == 60){
      break;
    }
  }
}
bool Timer::triggerMinute(){
  return triggerForMinute;
}
